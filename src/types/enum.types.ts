export enum WalletType {
	NONE,
	ALGOSIGNER,
	MY_ALGO,
	WALLET_CONNECT,
}

export enum ASA {
	NONE,
	ALGOSIGNER,
	MY_ALGO,
	WALLET_CONNECT,
}

export enum NavigationKey {
	VOTE,
	ADD_PROPOSAL,
}

export enum VoteOptions {
	ABSTAIN,
	YES,
	NO,
}

export enum ProposalType {
	ALGO = "algo",
	ASA = "asa",
	MESSAGE = "msg",
}
